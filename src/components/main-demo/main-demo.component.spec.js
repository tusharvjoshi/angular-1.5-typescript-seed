/*
describe('mainDemo', function() {

  // Test the controller
  describe('MainDemoController', function() {

    // Load the module that contains the `phoneList` component before each test
    beforeEach(module('mainDemo'));

     var $componentController;
     var scope;
     var ctrl;

    beforeEach(inject(function(_$componentController_, $rootScope){
      $componentController = _$componentController_;
      scope = $rootScope.$new();
      ctrl = $componentController('mainDemo');
    }));


    it('numCalc addition value', function() {
      expect(ctrl.numCalc.add).toBe(6);
    });

    it('numCalc addition value', function() {
      expect(ctrl.numCalc.subtract).toBe(1);
    });

    it('numCalc addition value', function() {
      expect(ctrl.numCalc.multiply).toBe(20);
    });

    it('numCalc addition value', function() {
      expect(ctrl.numCalc.divide).toBe(3);
    });

  });

});*/
